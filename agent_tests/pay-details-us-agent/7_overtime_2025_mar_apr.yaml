variables:
  employeeId: "E70007"
  currentPayDetailsId: 3000020

user_input: "Why didn’t my net pay double if I worked twice the hours?"

expected_answer: |
  Although your hours (and gross pay) doubled from 40 to 80, your net pay did not double because payroll taxes and contributions are not strictly linear. Federal, state, Social Security, and Medicare withholdings increased with the higher earnings, reducing the take-home rate on the additional income. Your pre-tax ($75.00 medical) and post-tax ($20.00 charity) deductions stayed flat, so they have less proportional impact at the higher gross, while progressive and proportional taxes drive a higher withholding share of the extra pay.

semantic_threshold: 0.80
comparison_method: semantic

tool_stubs:
  getPayDetails:
    - request:
        payDetailsId: 3000020
      method: GET
      path_template: "/{payDetailsId}"
      response_file: payslips/3000020.json

    - request:
        payDetailsId: 3000019
      method: GET
      path_template: "/{payDetailsId}"
      response_file: payslips/3000019.json

  getPayDetailsSummary:
    - request:
        employeeId: "E70007"
      method: GET
      path_template: "/employees/{employeeId}/summary"
      response_file: paySlipsSummary/overtime_2025_mar_apr.json


